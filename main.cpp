#include<bits/stdc++.h>
using namespace std;

int main()
{
    /* TODO the approach is to create a batch file-> this batch file compiles and executes a single testcase against an external cpp file we will execute the batch file using the system command -> the batch file uses another cpp program which fetches a testcase from the testcases file(this can be eliminated) then the output is shown to the user as a table with testcase its output and expected output */
}